<#@ template debug="false" hostspecific="false" language="C#" #>
<#@ assembly name="System.Core" #>
<#@ import namespace="System.Linq" #>
<#@ include file="Settings.tt" #>
<#@ import namespace="System.Text" #>
<#@ import namespace="System.Collections.Generic" #>
<#@ output extension=".h" #>
#pragma once
#include <stdint.h>
#include "Matrices.h"
#include "RowVectors.h"
#include "ColumnVectors.h"
#include "ColumnVectorsTranspose.h"
public:

<#

Func<int,int,string> simplebody = (x,y) => "return " + String.Join("+" + Environment.NewLine,Enumerable.Range(1, y).SelectMany(c =>  Enumerable.Range(1, x).Select(r => $"left.N{c} * right.M{r}.N{c}")));
Func<int,int,string> complexouter = (r,c) => String.Join(Environment.NewLine, 
Enumerable.Range(2, r-1).SelectMany(y=>
Enumerable.Range(2, c-1).Select((x)=> $"M_{x}x{1} Multiply(RV_{x} left, M_{y}x{x} right){{{simplebody(x,y)};}}"))
);

#><#=complexouter(dim,dim)#>